{
  "name": "@signal24/config",
  "version": "0.0.1",
  "description": "Runtime configuration helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "config-cli": "dist/bin/cli.js"
  },
  "scripts": {
    "dev": "ts-node src/cli.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "author": "Sean Ferguson <hello@sgnl24.com>",
  "license": "MIT",
  "dependencies": {
    "commander": "^9.4.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.11.10",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.1",
    "type-fest": "^3.3.0",
    "typescript": "^4.9.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
